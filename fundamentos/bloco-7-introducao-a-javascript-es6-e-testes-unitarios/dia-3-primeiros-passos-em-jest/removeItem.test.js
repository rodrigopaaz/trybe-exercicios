const myRemove = require('.removeItem.js');
const { it } = require('node:test');
const { describe } = require('yargs');

describe('testando array:', () => {
    it('verifica se o array contém a informação', () => {
        expect()
    })
})
